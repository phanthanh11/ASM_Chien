{% extends 'base.html.twig' %}

{% block title %}Theloai{% endblock %}

{% block body %}
    <h1>Theloai</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theloai.id }}</td>
            </tr>
            <tr>
                <th>Tentheloai</th>
                <td>{{ theloai.tentheloai }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_theloai_index') }}">back to list</a>

    <a href="{{ path('app_theloai_edit', {'id': theloai.id}) }}">edit</a>

    {{ include('theloai/_delete_form.html.twig') }}
{% endblock %}
